{
    "private": true,
    "scripts": {
        "setup" :"docker run --rm -v $(pwd):/opt -w /opt laravelsail/php80-composer:latest ; composer install --ignore-platform-reqs",
        "dev": "npm run development"
    },
    "devDependencies": {
        "axios": "^0.21",
        "laravel-mix": "^6.0.6",
        "lodash": "^4.17.19",
        "postcss": "^8.1.14"
    }
}
